# The game's Claude project

The bootstrap fills the placeholders and prints these for pasting. `instructions.md` goes in the Claude project's *Project instructions* field. Each `chat-*.md` is the first message of a new chat in that project. Upload the token file and (for reference) `GDD.md` to the project's files.
