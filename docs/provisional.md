# Provisional register

Every placeholder assumption implemented against an `OPEN` rule (CLAUDE.md 1.6). One row per marker. `tier1` fails when a marker exists without a row or a row without a marker. Asset rows: the row is the marker.

| Decision | What was assumed | Why now | Marker (file path, or `asset <path>`) | Since | Resolves when |
|---|---|---|---|---|---|
